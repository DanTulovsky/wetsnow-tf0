# https://artifacthub.io/packages/helm/prometheus-community/prometheus-pushgateway

resources:
  limits:
    cpu: 100m
    memory: 300Mi
  requests:
    cpu: 10m
    memory: 30Mi

serviceMonitor:
  enabled: true
