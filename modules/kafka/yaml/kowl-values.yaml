# https://github.com/cloudhut/charts
secret:
  existingSecret: ${kowlSecret}
kowl:
  config:
    kafka:
      brokers:
        - ${broker}

resources:
  limits:
    cpu: 50m
    memory: 128Mi
  requests:
    cpu: 10m
    memory: 64Mi
